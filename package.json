{
  "name": "rovocms",
  "description": "Move fast. Edit safe. The CMS that doesn't get in your way.",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot server.ts",
    "start": "bun run server.ts"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "@hono/node-server": "^1.13.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/better-sqlite3": "^7.6.12"
  }
}